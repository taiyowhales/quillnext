"use client";

import { CheckCircle as PhosphorCheckCircle } from "@phosphor-icons/react";
import type { IconProps } from "@phosphor-icons/react";

export function CheckCircle(props: IconProps) {
  return <PhosphorCheckCircle {...props} />;
}

