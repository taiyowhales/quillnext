export const dynamic = "force-dynamic";
export const runtime = "nodejs";

import { GET, POST } from "@/auth";

/**
 * Auth.js API route handler
 * Handles all authentication requests
 * 
 * GET and POST are exported directly from @/auth
 */
export { GET, POST };

